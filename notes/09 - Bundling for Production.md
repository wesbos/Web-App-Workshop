## Ready to deploy to Production?

Parcel will automatically bundle and minify your CSS (or Sass!), JS and HTML for you.

Just run `npm run build` which in turns runs `parcel build index.html`.

Let's do that and look at the outputted files.

(It's really that simple!)
